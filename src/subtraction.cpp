#include "subtraction.h"
#include <iostream>
double subtraction(const double &first_value, const double &seconde_value) {
  printf("subtraction value is %lf \n", seconde_value - first_value);
  return seconde_value - first_value;
}